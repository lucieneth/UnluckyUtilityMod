package unlucky.utility.client.util;

/** Marks the synthetic, head-only render state appended for Freecam F5. */
public interface FreecamProxyRenderState {
	void unlucky$setFreecamProxy(boolean proxy);

	boolean unlucky$isFreecamProxy();
}
